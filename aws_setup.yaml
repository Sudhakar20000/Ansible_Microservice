- name: aws_setup
  hosts: local
  connection: local
  vars:
    ami_id: ami-0220d79f3f480ecf5
    instance: ["mysql","mq"]
    act: create
  tasks:
    - name: crate instance
      amazon.aws.ec2_instance:
        name: "roboshop-{{item}}"
        instance_type: t3.micro
        image_id: "{{ami_id}}"
        security_groups:
          - "roboshop-{{item}}"
        state: present
      loop: "{{ instance }}"
      register: output
      when: act == "create"
    - name: print the output
      debug:
        msg: "{{ output }}"
